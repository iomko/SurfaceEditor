# ViewPortLayerRenderSettings
Stores shader configurations used for rendering various elements of a viewport layer, such as meshes, edges, and faces.

This class holds pointers to shader programs that define how each visual component of the scene is rendered.
