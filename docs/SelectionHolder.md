# SelectionHolder
The SelectionHolder struct serves as a centralized container that stores the current selection state of meshes and their faces.
It keeps track of which meshes are selected and, for each selected mesh, which faces are selected within that mesh.
